# Server configuration
server.port=8080 

spring.datasource.username=root
spring.datasource.password=Rithu@06
spring.datasource.url=jdbc:mysql://localhost:3306/donate
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver


# Hibernate (JPA) settings
spring.jpa.hibernate.ddl-auto=update  
spring.jpa.show-sql=true  
spring.jpa.properties.hibernate.format_sql=true  
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Enable logging for SQL queries (optional)
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# Allow bean definition overriding (if needed)
spring.main.allow-bean-definition-overriding=true


spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB


# Connection pool settings (optional, but recommended for production)
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5

# Enable CORS support globally (if it's still an issue with frontend)
spring.webflux.cors.enabled=true


# Email Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=trishithagattam@gmail.com
spring.mail.password=tpgb irho xkmw jaly
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true





